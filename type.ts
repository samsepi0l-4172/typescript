const player: readonly [string, number, boolean] = ['sepi', 12, true];

let a: undefined = undefined;
let b: null = null;

type Player = {
  age?: number;
};

let c = [];

const d: any[] = [1, 2, 3, 4];
const e: any = true;
d + e;
